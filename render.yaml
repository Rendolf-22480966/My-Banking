services:
  - type: web
    name: first-choice-credit-union
    runtime: static
    buildCommand: echo "static site"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /
        destination: /index.html
